# Future Kubernetes Deployment Template for AdPulse Vue.js Frontend
# Target: Azure Kubernetes Service (AKS)
# Alternative (recommended): Deploy the built static assets to Azure Blob Storage + Azure Front Door (CDN)
# Images should be sourced from Azure Container Registry (ACR): <registry>.azurecr.io/adpulse-frontend:latest
apiVersion: apps/v1
kind: Deployment
metadata:
  name: adpulse-frontend
  namespace: adpulse
  labels:
    app: adpulse-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: adpulse-frontend
  template:
    metadata:
      labels:
        app: adpulse-frontend
    spec:
      containers:
      - name: frontend
        image: adpulse-frontend:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: adpulse-frontend
  namespace: adpulse
spec:
  selector:
    app: adpulse-frontend
  ports:
  - port: 80
    targetPort: 80
  type: LoadBalancer
